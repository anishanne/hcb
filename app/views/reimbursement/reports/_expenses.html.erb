<% if @ordered_expenses.any? %>
  <% @ordered_expenses.each do |expense| %>
    <%= render "reimbursement/expenses/expense", expense:, new: @editing == expense.id, read_only: @read_only %>
  <% end %>
<% end %>
