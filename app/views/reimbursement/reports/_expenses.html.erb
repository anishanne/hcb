<% if report.expenses.any? %>
  <% report.expenses.sort_by(&:created_at).each do |expense| %>
    <%= render "reimbursement/expenses/expense", expense:, new: @editing == expense.id %>
  <% end %>
<% end %>
